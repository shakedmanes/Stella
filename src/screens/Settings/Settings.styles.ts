import { StyleSheet } from "react-native";
import normalize from 'react-native-normalize';

const styles = StyleSheet.create({
    mainContainer: {
        margin: normalize(25, 'width')
    }    
});

export default styles;